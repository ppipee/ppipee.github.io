import styled from 'styled-components'

import Gap from 'common/components/Gap'

export const SkillList = styled.div`
	display: flex;
	flex-wrap: wrap;
`

export const SkillContainer = styled(Gap)`
	padding: 0 16px;
`
